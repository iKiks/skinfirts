import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const Color primary = Color(0xFF2260FF);
  static const Color secondary = Color(0xFFCAD6FF);
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);

  // Muted error tone to avoid clashing with the blue palette.
  static const Color error = Color(0xFFF07A94);

  static const Color background = Color(0xFFF4F7FF);
  static const Color surface = Color(0xFFEEF3FF);
  static const Color textPrimary = Color(0xFF10121B);
  static const Color textSecondary = Color(0xFF4F566C);
  static const Color border = Color(0xFFB5C2F7);
}
